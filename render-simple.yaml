# Simple Render deployment without nested directories
services:
  - type: web
    name: shadowid-backend
    runtime: node
    buildCommand: npm install --prefix backend
    startCommand: npm start --prefix backend
    healthCheckPath: /health
    plan: starter
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000